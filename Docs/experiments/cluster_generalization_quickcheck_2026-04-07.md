# 机制簇内 / 跨簇泛化快速验证（2026-04-07）

- 状态：`current`
- 首次确认：`2026-04-07`
- 最近复核：`2026-04-07`
- 数据范围：
  - 目标工况：`工况1 / 3 / 6 / 8 / 15 / 16 / 17 / 18`
- 代码口径：
  - `src/try/032_cluster_generalization_quickcheck/`
- 证据入口：
  - `outputs/try/032_cluster_generalization_quickcheck/case_level_results.csv`
  - `outputs/try/032_cluster_generalization_quickcheck/summary.csv`
  - `outputs/try/032_cluster_generalization_quickcheck/summary.md`

## 1. 目标

在第一层机制簇已经形成的前提下，快速验证：

- 同簇训练是否比跨簇训练更容易泛化；
- 机制簇是否真的对 `TinyTCN@5s` 的泛化有解释力。

## 2. 方法口径

- 模型：`TinyTCN@5s`
- 评估工况：
  - `工况1 / 3 / 6 / 8 / 15 / 16 / 17 / 18`
- 训练池比较：
  - `full`
  - `same_cluster_matched`
  - `cross_cluster_matched`
- 为了避免样本数混淆：
  - `same / cross` 使用匹配后的训练工况数

## 3. 当前结果

### 3.1 [2026-04-07] 全量训练仍然整体最稳

- 状态：`current`
- 首次确认：`2026-04-07`
- 最近复核：`2026-04-07`

总览：

- `full`
  - `case_mae = 0.3763`
- `cross_cluster_matched`
  - `case_mae = 1.1149`
- `same_cluster_matched`
  - `case_mae = 1.1179`

这说明：

- 当前数据规模下，直接砍成小训练池会明显损失稳定性；
- 不能简单把“簇内训练”当成当前默认训练策略。

### 3.2 [2026-04-07] 对 `cluster 1` 的若干工况，同簇训练明显优于跨簇训练

- 状态：`current`
- 首次确认：`2026-04-07`
- 最近复核：`2026-04-07`

代表结果：

- `工况15`
  - `same = 0.0962`
  - `cross = 2.0173`
- `工况16`
  - `same = 0.1743`
  - `cross = 2.6787`
- `工况17`
  - `same = 0.0953`
  - `cross = 2.0208`

这说明：

- `15 / 16 / 17` 这类工况已经具有明显的子域特征；
- 用别簇工况训练去预测它们，会明显更难。

### 3.3 [2026-04-07] `工况18` 仍然是特殊点，不能简单视作普通 `cluster 1` 工况

- 状态：`current`
- 首次确认：`2026-04-07`
- 最近复核：`2026-04-07`

`工况18` 的结果：

- `cross = 0.4258`
- `full = 0.8629`
- `same = 6.2097`

这说明：

- `工况18` 在机制簇上属于 `cluster 1`；
- 但在泛化表现上，它并不服从“同簇更好”这一规律；
- 当前它更像是一个需要单独解释的特殊工况。

## 4. 当前判断

`2026-04-07` 的这轮快速验证支持以下判断：

- 机制簇对泛化是有解释力的，但不是全局统一规律；
- `15 / 16 / 17` 已经可以被看作一个更独立的子域；
- `工况18` 仍是特殊点；
- 如果接下来要检查标签问题，最值得重点盯的是：
  - `工况1`
  - `工况18`
