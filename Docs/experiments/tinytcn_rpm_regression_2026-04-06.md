# TinyTCN 转速回归探索（2026-04-06）

- 状态：`current`
- 首次确认：`2026-04-06`
- 最近复核：`2026-04-06`
- 数据范围：manifest 迁移后的 `工况1` 到 `工况20`
- 代码口径：
  - `src/try/019_tinytcn_rpm_regression/`
- 证据入口：
  - `outputs/try/019_tinytcn_rpm_regression/model_summary.csv`
  - `outputs/try/019_tinytcn_rpm_regression/case_level_predictions.csv`
  - `outputs/try/019_tinytcn_rpm_regression/unlabeled_predictions.csv`

## 1. 目标

在 `2026-04-06` 当前第三阶段原始时序口径下：

- 复用与风速回归相同的清洗、切窗和 `Leave-One-Condition-Out` 评估；
- 继续使用 `TinyTCN`；
- 把监督目标从 `wind_speed` 切换为 `rpm`，验证这条端到端路线能否稳定做工况级转速回归。

## 2. 方法口径

- 清洗逻辑：复用 `src/current.data_loading`
- 原始窗口构造：复用 `src/try/012_phase3_end_to_end_shortlist/phase3_end_to_end_lib.py`
- 默认窗口参数：
  - 采样率：`50Hz`
  - 窗长：`5s`
  - 步长：`2.5s`
- 监督目标：`dataset_manifest.csv` 中的 `rpm`
- 评估方式：
  - 分组单位为工况
  - `Leave-One-Condition-Out`
- 训练配置：
  - batch size：`32`
  - max epochs：`40`
  - patience：`8`
  - learning rate：`1e-3`
  - 设备：`CPU`

## 3. 当前结果

### 3.1 [2026-04-06] TinyTCN 已可在当前口径下稳定跑通 rpm 回归

- 状态：`current`
- 首次确认：`2026-04-06`
- 最近复核：`2026-04-06`

全量 `LOCO` 结果：

- `case_mae = 8.3832 rpm`
- `case_rmse = 13.7483 rpm`
- `case_mape = 5.8613%`

无标签工况推理：

- `工况2.csv` 预测转速：`152.8107 rpm`

### 3.2 [2026-04-06] 当前误差主要由少数工况主导

- 状态：`current`
- 首次确认：`2026-04-06`
- 最近复核：`2026-04-06`

绝对误差较大的工况包括：

- `工况1`：`42.6483 rpm`
- `工况18`：`27.9731 rpm`
- `工况20`：`22.3007 rpm`

其余多数工况已经落在更小的误差范围内，这说明：

- 当前路线不是整体失效；
- 但跨工况泛化还会被少数分布差异更大的工况明显拉低。

## 4. 当前不确定点

### 4.1 [2026-04-06] 还未确认 rpm 是否比 wind_speed 更依赖低频周期信息

- 状态：`current`
- 首次确认：`2026-04-06`
- 最近复核：`2026-04-06`

当前 rpm 回归先直接沿用了风速回归的 `5s / 2.5s` 切窗口径。

仍待确认：

- rpm 任务是否更适合更长窗长；
- 是否需要显式保留更强的周期性线索；
- `014` 中的窗长扫描结论能否直接迁移到 rpm 目标。

### 4.2 [2026-04-06] 还未确认工况级常数标签是否会放大跨工况偏移问题

- 状态：`current`
- 首次确认：`2026-04-06`
- 最近复核：`2026-04-06`

当前 `rpm` 标签仍是工况级常数，而模型输入是窗口级原始时序。

这意味着：

- 训练时所有窗口共享同一个工况标签；
- 如果不同工况的稳态程度、缺失切分位置或能量分布不同，模型可能学到“工况特征”而不只是“转速特征”。

这类风险在 `工况1 / 18 / 20` 的大误差上值得优先复核。

### 4.3 [2026-04-06] 当前还没有 rpm 任务的对照基线

- 状态：`current`
- 首次确认：`2026-04-06`
- 最近复核：`2026-04-06`

这次 try 只验证了 `TinyTCN` 能否跑通并给出合理误差。

仍待补充：

- 直接用显式频域特征做 rpm 回归；
- 用同口径 `Tiny1DCNN` 做并列对照；
- 对比更长窗长或更低步长下的收益是否显著。
